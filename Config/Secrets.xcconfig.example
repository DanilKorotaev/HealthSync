// Copy this file to Secrets.xcconfig in the same folder (Secrets.xcconfig is gitignored).
// Values here are injected at build time as INFOPLIST_KEY_* or user-defined settings if you wire them in Xcode.
// Prefer runtime configuration via the app Settings screen and Keychain for credentials.

// Example (optional): override display name for local builds only
// INFOPLIST_KEY_CFBundleDisplayName = HealthSync Local
