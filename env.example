# Copy to .env for local tooling/scripts only (not used by the iOS runtime directly).
# The iOS app reads the same variable NAMES via Xcode scheme Environment Variables
# or in-app Settings (see docs/SETUP.md).

HEALTHSYNC_NEXTCLOUD_BASE_URL=
HEALTHSYNC_NEXTCLOUD_WEBDAV_ROOT=
HEALTHSYNC_SYNC_WEBHOOK_URL=
